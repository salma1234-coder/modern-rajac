# دليل نظام المصادقة - منصة التمريض

## نظرة عامة
تم إضافة نظام مصادقة آمن لمنصة التمريض مع الحفاظ على التصميم والهيكل الأصليين للمنصة.

## الميزات المضافة

### 1. نظام تسجيل الدخول الآمن
- تخزين أسماء المستخدمين وكلمات المرور في قاعدة بيانات منفصلة (database.json)
- تشفير كلمات المرور باستخدام bcrypt
- إدارة الجلسات الآمنة
- تسجيل جميع عمليات الدخول مع التاريخ والوقت وعنوان IP

### 2. لوحة تحكم المسؤول
- عرض إحصائيات شاملة (عدد المستخدمين، الطلاب، عمليات الدخول)
- إدارة المستخدمين (إضافة، تعديل، حذف، بحث)
- عرض سجلات الدخول
- تصدير البيانات إلى CSV

### 3. الحماية
- حماية صفحة المسؤول من الوصول غير المصرح به
- التحقق من صلاحيات المستخدم قبل الوصول إلى الوظائف الإدارية
- انتهاء صلاحية الجلسة بعد 24 ساعة

## بيانات الدخول الافتراضية

### حساب المسؤول
- **اسم المستخدم:** admin
- **كلمة المرور:** admin123

⚠️ **مهم:** يرجى تغيير كلمة مرور المسؤول بعد أول تسجيل دخول

## كيفية الاستخدام

### تسجيل الدخول كطالب
1. افتح المنصة على http://localhost:3000
2. أدخل اسم المستخدم وكلمة المرور
3. انقر على "دخول الطالب"
4. سيتم عرض اسمك بعد تسجيل الدخول الناجح

### تسجيل الدخول كمسؤول
1. افتح المنصة على http://localhost:3000
2. أدخل اسم المستخدم: admin
3. أدخل كلمة المرور: admin123
4. بعد تسجيل الدخول، سيظهر رابط "لوحة التحكم"
5. انقر على الرابط للوصول إلى لوحة التحكم

### إضافة مستخدم جديد (من لوحة التحكم)
1. سجل الدخول كمسؤول
2. انتقل إلى "إدارة المستخدمين"
3. انقر على "إضافة مستخدم جديد"
4. املأ البيانات المطلوبة:
   - اسم المستخدم (مطلوب)
   - الاسم الكامل (مطلوب)
   - كلمة المرور (مطلوب)
   - الصف الدراسي (اختياري)
   - النوع (اختياري)
   - الفصل (اختياري)
   - الدور (طالب أو مسؤول)
5. انقر على "حفظ"

### تعديل مستخدم
1. من لوحة التحكم، انتقل إلى "إدارة المستخدمين"
2. ابحث عن المستخدم
3. انقر على "تعديل"
4. قم بتعديل البيانات المطلوبة
5. انقر على "حفظ"

### حذف مستخدم
1. من لوحة التحكم، انتقل إلى "إدارة المستخدمين"
2. ابحث عن المستخدم
3. انقر على "حذف"
4. أكد الحذف

### البحث عن مستخدم
1. من لوحة التحكم، انتقل إلى "إدارة المستخدمين"
2. أدخل اسم المستخدم أو الاسم الكامل أو الفصل في مربع البحث
3. انقر على "بحث"

### تصدير البيانات
- **تصدير المستخدمين:** من لوحة التحكم، انتقل إلى "إدارة المستخدمين" وانقر على "تصدير المستخدمين (CSV)"
- **تصدير سجلات الدخول:** من لوحة التحكم، انتقل إلى "سجلات الدخول" وانقر على "تصدير سجلات الدخول (CSV)"

### تسجيل الخروج
- انقر على زر "تسجيل خروج" في أي صفحة

## الملفات المضافة/المعدلة

### الملفات الجديدة
1. **database.json** - قاعدة البيانات لتخزين المستخدمين وسجلات الدخول
2. **admin.html** - صفحة لوحة تحكم المسؤول
3. **admin.js** - كود JavaScript لوحة التحكم
4. **AUTHENTICATION_GUIDE.md** - هذا الملف

### الملفات المعدلة
1. **server.js** - تم تحديثه بنظام المصادقة وAPI endpoints
2. **SS.html** - تم تعديل نموذج تسجيل الدخول
3. **script.js** - تم تحديث وظيفة تسجيل الدخول والخروج

## API Endpoints

### المصادقة
- `POST /api/login` - تسجيل الدخول
- `POST /api/logout` - تسجيل الخروج
- `GET /api/user` - الحصول على بيانات المستخدم الحالي

### إدارة المستخدمين (للمسؤولين فقط)
- `GET /api/admin/users` - الحصول على جميع المستخدمين
- `POST /api/admin/users` - إضافة مستخدم جديد
- `PUT /api/admin/users/:id` - تعديل مستخدم
- `DELETE /api/admin/users/:id` - حذف مستخدم
- `GET /api/admin/users/search?query=...` - البحث عن مستخدمين

### سجلات الدخول (للمسؤولين فقط)
- `GET /api/admin/login-records` - الحصول على جميع سجلات الدخول

### التصدير (للمسؤولين فقط)
- `GET /api/admin/export/users` - تصدير المستخدمين إلى CSV
- `GET /api/admin/export/login-records` - تصدير سجلات الدخول إلى CSV

## الأمان

### كلمات المرور
- يتم تشفير جميع كلمات المرور باستخدام bcrypt
- لا يتم تخزين كلمات المرور بنص واضح

### الجلسات
- تنتهي الجلسات تلقائياً بعد 24 ساعة
- يتم تخزين الجلسات بشكل آمن على الخادم

### الحماية
- يتم حماية جميع API endpoints الإدارية
- يتم التحقق من صلاحيات المستخدم قبل تنفيذ العمليات
- يتم حماية صفحة المسؤول من الوصول المباشر

## القابلية للتوسع

النظام مصمم لدعم 3000+ مستخدم:
- استخدام lowdb لقاعدة بيانات خفيفة وسريعة
- تحسين الأداء مع Pagination
- إدارة فعالة للجلسات
- بنية قابلة للترقية إلى قاعدة بيانات أكبر (MongoDB, PostgreSQL) إذا لزم الأمر

## استكشاف الأخطاء وإصلاحها

### لا يمكن تسجيل الدخول
- تأكد من صحة اسم المستخدم وكلمة المرور
- تأكد من أن الخادم يعمل (npm start)
- تحقق من console في المتصفح للأخطاء

### لا يمكن الوصول إلى لوحة التحكم
- تأكد من تسجيل الدخول كمسؤول
- تأكد من أن حسابك له دور "admin"
- تحقق من أنك تستخدم الرابط الصحيح

### أخطاء في قاعدة البيانات
- تأكد من وجود ملف database.json
- تأكد من أن الخادم لديه صلاحيات القراءة/الكتابة
- إذا كان الملف تالفاً، احذفه وسيتم إنشاؤه تلقائياً

## التوصيات

### للإنتاج
1. غيّر كلمة مرور المسؤول الافتراضية
2. استخدم متغير بيئة SESSION_SECRET قوي
3. فعّل HTTPS
4. استخدم قاعدة بيانات احترافية (MongoDB, PostgreSQL)
5. أضف حدًا لمحاولات تسجيل الدخول الفاشلة
6. فعّل CORS إذا لزم الأمر

### للصيانة
1. قم بعمل نسخة احتياطية لقاعدة البيانات بانتظام
2. راجع سجلات الدخول بشكل دوري
3. قم بتحديث كلمات المرور بشكل دوري
4. احذف المستخدمين غير النشطين

## الدعم

إذا واجهت أي مشاكل أو كان لديك أسئلة، يرجى مراجعة:
- ملف README.md الرئيسي
- سجلات الخادم في terminal
- console في المتصفح للأخطاء

---

**ملاحظة:** تم الحفاظ على التصميم والهيكل الأصليين للمنصة. جميع الميزات الجديدة مدمجة بشكل سلس مع الكود الموجود.
